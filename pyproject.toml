[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "browserskill-capsolver-integration"
version = "0.2.0"
description = "Bounded CAPTCHA handling for authorized BrowserSkill tasks"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
